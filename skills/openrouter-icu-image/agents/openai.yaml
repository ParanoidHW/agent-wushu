interface:
  display_name: "OpenRouter ICU Image"
  short_description: "Generate images, candidates, edits, and uploaded-document visuals with OpenRouter ICU"
  default_prompt: "Use $openrouter-icu-image to generate image candidates, edit visual references, or create an image from an uploaded document through OpenRouter ICU."
